# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.vs/
*.user
*.suo

# SQL Server / SQL tools
*.bak
*.trn
*.mdf
*.ldf
*.ndf
*.bacpac
*.dacpac
*.sqlplan
*.queryanalysis
sqlproj.user

# SSMS / Azure Data Studio
*.dbmdl

# .NET (for full-stack follow-ups)
bin/
obj/
*.dll
*.exe
*.pdb
TestResults/
coverage*.xml
coverage*.json

# Node (for full-stack follow-ups)
node_modules/
dist/
.next/
out/
.env
.env.*
npm-debug.log*

# Logs / temp
*.log
# query-testing run logs are verification evidence — keep them tracked
!query-testing/results/*.log
*.tmp
*.temp
tmp/
temp/

# Graphify (copied from CollegeAdmissionManagementSystem .gitignore)
# graphify machine-local sidecars (interpreter path + scan root differ per
# machine; the skill regenerates both on every run, so never commit them)
graphify-out/.graphify_python
graphify-out/.graphify_root
# graphify local-only outputs (graphify-out/ itself is meant to be committed,
# but these are machine-local / regenerable and must stay out of git)
graphify-out/cost.json
graphify-out/cache/
graphify-out/last_query_stamp
# graphify curated-graph backups (auto-created dated snapshots on every
# `graphify update`; regenerable, only bloat the repo)
graphify-out/20*/
